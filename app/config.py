# config.py
MILVUS_HOST = "localhost"
MILVUS_PORT = "19530"
# DEVICE = "cuda" if torch.cuda.is_available() else "cpu"
# FACE_RECOGNITION_MODEL = "casia-webface"  # No need for a path if using pre-trained models
